# 虚拟患者进课堂 - 虚拟形象问答系统 招标技术参数表

## 1.2 技术和性能参数

### 1.2.1 虚拟形象生成

| 序号 | 功能模块 | 技术参数要求 |
|------|---------|-------------|
| | **系统基础架构** | （1）支持基于React 18+的现代化前端架构，支持TypeScript类型安全开发；<br>（2）支持Material-UI (MUI) 5.x+企业级UI组件库，提供统一的界面设计规范；<br>（3）支持Vite或Webpack构建工具，开发环境热更新响应时间<500ms；<br>（4）支持RESTful API接口规范，前后端分离架构；<br>（5）支持Docker容器化部署，便于环境迁移和扩展；<br>（6）支持HTTPS安全传输协议，保障数据传输安全。 |
| | **唇形同步引擎** | （7）支持基于深度学习的实时唇形同步技术，如MuseTalk、Wav2Lip等开源框架；<br>（8）支持潜空间修复（Latent Space Inpainting）技术，提升生成质量；<br>（9）支持Whisper等音频特征提取模型，实现精准的音频-视觉对齐；<br>（10）支持VAE（变分自编码器）进行面部图像编码解码；<br>（11）唇形同步准确度（LSE-D）≤8.0，同步置信度（LSE-C）≥7.0；<br>（12）支持中文、英文、日文等多语言音频输入。 |
| | **面部动画生成** | （13）支持MediaPipe或InsightFace等面部识别技术，自动识别定位人脸；<br>（14）支持478个面部特征点精准定位（Face Mesh技术）；<br>（15）支持3DMM（3D可变形模型）参数映射，分离表情、姿态、形状系数；<br>（16）支持bbox_shift等参数调整，精细控制嘴部开合程度；<br>（17）身份保持度（CSIM）≥0.85，采用ArcFace余弦相似度评估；<br>（18）面部图像质量（FID）≤25，确保生成图像自然度。 |
| | **角色形象管理** | （19）支持多种医疗角色形象，包括但不限于医生、护士、患者等；<br>（20）支持自定义角色训练，可基于提供的图片素材生成新角色；<br>（21）支持角色形象库管理，便于统一维护和调用；<br>（22）支持半身、全身等多种角色展示形式。 |

### 1.2.2 语音合成

| 序号 | 功能模块 | 技术参数要求 |
|------|---------|-------------|
| | **文本处理** | （23）支持中文医学术语的准确分词处理，可扩展医学专用词典；<br>（24）支持jieba等开源分词工具，支持自定义词典加载；<br>（25）支持BERT等语义编码模型，实现韵律节奏自然分析；<br>（26）支持医学专业词汇的自定义发音标注。 |
| | **语音克隆** | （27）支持少样本语音克隆技术，如GPT-SoVITS、CosyVoice等开源框架；<br>（28）5秒音频样本即可实现80%以上音色相似度；<br>（29）1分钟音频样本可达到接近真人的语音效果；<br>（30）支持中英日韩粤等多语言语音合成。 |
| | **语音质量** | （31）语音合成主观平均意见分（MOS）≥4.0（满分5.0）；<br>（32）支持VITS2等神经网络声码器，确保语音自然流畅；<br>（33）支持48kHz高采样率音频输出；<br>（34）医学术语发音错误率<5%。 |

### 1.2.3 视频生成与处理

| 序号 | 功能模块 | 技术参数要求 |
|------|---------|-------------|
| | **视频生成** | （35）支持基于音频自动生成对应的虚拟形象视频；<br>（36）音画同步误差<100ms；<br>（37）支持720p、1080p等多种分辨率输出；<br>（38）面部区域处理分辨率支持256×256或512×512。 |
| | **格式支持** | （39）输入图像格式支持：JPG、PNG、WebP，建议分辨率512×512以上；<br>（40）输入音频格式支持：WAV、MP3、M4A、FLAC，采样率16kHz以上；<br>（41）输出视频格式支持：MP4（H.264/H.265）、WebM（VP9）、MOV；<br>（42）输出音频格式支持：WAV（48kHz）、MP3（320kbps）。 |
| | **视频处理** | （43）支持FFmpeg等开源工具进行视频格式转换和处理；<br>（44）支持视频分段生成，支持30分钟以上长视频制作；<br>（45）支持批量视频生成任务队列管理。 |

### 1.2.4 性能指标

| 序号 | 功能模块 | 技术参数要求 |
|------|---------|-------------|
| | **推理性能** | （46）专业GPU（NVIDIA V100/RTX 3090）环境下，视频生成帧率≥30FPS；<br>（47）消费级GPU（RTX 3060/4060）环境下，视频生成帧率≥15FPS；<br>（48）单视频生成实时因子（RTF）<0.5，即生成速度≥2倍实时；<br>（49）语音合成实时因子（RTF）≈0.03，即生成速度约33倍实时；<br>（50）实时对话场景首帧延迟<500ms。 |
| | **系统稳定性** | （51）系统可用率≥99.5%；<br>（52）支持24小时连续运行无崩溃；<br>（53）支持≥10个并发视频生成任务；<br>（54）用户界面响应时间<200ms。 |

### 1.2.5 内容管理

| 序号 | 功能模块 | 技术参数要求 |
|------|---------|-------------|
| | **FAQ管理** | （55）支持FAQ问答内容的结构化管理，采用JSON或数据库存储；<br>（56）支持问题分类、标签管理功能；<br>（57）支持问答内容的增删改查操作；<br>（58）支持多套FAQ数据集切换（如住培生内容、患者FAQ等）。 |
| | **视频资源管理** | （59）支持视频文件的统一存储和管理；<br>（60）支持视频与FAQ问题的关联映射；<br>（61）支持视频预览和播放功能；<br>（62）支持视频文件的批量导入导出。 |
| | **用户界面** | （63）支持问题列表展示，包含展开/收起功能；<br>（64）支持视频播放器集成，选择问题后自动播放对应视频；<br>（65）支持响应式布局，适配不同屏幕尺寸；<br>（66）界面支持中文显示，符合医学教育场景需求。 |

### 1.2.6 硬件环境要求

| 序号 | 配置项 | 最低配置 | 推荐配置 |
|------|-------|---------|---------|
| | **GPU** | NVIDIA RTX 3060 12GB | NVIDIA RTX 4090 24GB |
| | **CPU** | Intel i5-12400 / AMD R5 5600 | Intel i7-13700 / AMD R7 7800X |
| | **内存** | 16GB DDR4 | 32GB DDR5 |
| | **存储** | 256GB SSD | 1TB NVMe SSD |
| | **CUDA版本** | 11.7+ | 12.1+ |
| | **Python版本** | 3.10 | 3.11 |
| | **操作系统** | Ubuntu 20.04 / Windows 10 | Ubuntu 22.04 / Windows 11 |

### 1.2.7 交付要求

| 序号 | 交付项 | 要求说明 |
|------|-------|---------|
| （67） | 系统应用程序 | React前端应用 + 视频生成后端服务，可独立部署运行 |
| （68） | AI模型文件 | 唇形同步模型（MuseTalk等）、语音合成模型（GPT-SoVITS等）预训练权重 |
| （69） | 医学角色资源 | 至少包含医生、护士2种角色的形象图片和语音样本 |
| （70） | 用户使用说明 | 操作手册、使用教程（图文或视频形式） |
| （71） | 技术部署文档 | Docker部署指南、API接口文档、系统架构说明 |
| （72） | 测试验收报告 | 功能测试报告、性能测试报告、质量评估报告 |
| （73） | 源代码 | 完整代码仓库，包含前端、后端、配置文件等 |
| （74） | 培训服务 | 提供系统使用和基础维护培训 |

---

**文档版本：** v1.0
**编制日期：** 2025年12月
**文档路径：** `/docs/招标技术参数表.md`

*Generated with [Claude Code](https://claude.ai/code)*
